---
import { Image } from '@astrojs/image/components'
import type {
	ImageComponentLocalImageProps,
	ImageComponentRemoteImageProps,
} from '@astrojs/image/components'

export type Props = ImageComponentLocalImageProps | ImageComponentRemoteImageProps

const { alt, slot, src, ...rest } = Astro.props
---

<Image alt={alt} src={src} {...rest} />
